# 个人工作台 · 部署说明

## 访问地址（固定，手机电脑通用）
**https://lulu-hub-44.github.io/personal-workbench/**

电脑、手机浏览器直接打开同一个网址即可，无需登录。

## 已实现功能
- 四大模块：体重管理、心情指数、工作情况、月度总览
- 体重分析：热量超标 / 低热量双线归因，联动心情与工作，热量失控预警
- 排卵期储水等生理期水位模型
- 数据导入 / 导出（JSON、CSV，支持合并或替换）
- **☁ 云同步（已可用）**：基于 GitHub 私有仓库，手机电脑共享数据
- PWA 可“添加到主屏幕”当 App 用

## ☁ 云同步使用步骤（手机和电脑各做一次）
1. 打开网页，点右上角 **☁ 云同步**
2. 在“GitHub 私人令牌”里粘贴你的令牌（`ghp_...`，需要 **repo** 权限）
3. 点 **上传到云端**（本机数据推到 GitHub 私有仓库 `pw-sync-store`）
4. 在另一台设备同样填令牌，点 **从云端下载**（拉取并覆盖本机）
5. 如需自动同步，勾选“自动同步”即可

> 首次同步会自动在你的 GitHub 创建私有仓库 `pw-sync-store`（数据仅你可见）。
> 多个“同步槽”可隔离不同数据集；留空默认 `default`。

## 关于那把 GitHub 令牌（token）
- 云同步**依赖这把令牌常驻**，请勿在 GitHub 撤销（撤销后云同步停止）。
- 令牌仅保存在你浏览器本地（localStorage），不传给我、不上传任何服务器。
- 它只有 `repo` 权限（可访问你账号下的仓库）；若担心，可单独建一个 GitHub 小号专门放同步仓库。
- 手机和电脑填**同一把**令牌，才能同步彼此数据。

## 技术状态
- 纯静态站点（相对路径、Service Worker scope `./`、含 `.nojekyll`），GitHub Pages 托管，无后端依赖
- 云同步后端：GitHub Contents API → 私有仓库 `pw-sync-store` / `sync.json`（base64 存储，支持中文）
- 仓库：`Lulu-hub-44/personal-workbench`（`master` 分支根目录为网页源）

## 以后怎么更新
改代码后告诉我“重新部署”，我会 push 并等 Pages 重建（约几十秒生效）。
