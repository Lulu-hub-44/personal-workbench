# 个人工作台 · 部署说明

## 访问地址（固定，国内可访问）
**https://lulu-hub-44.github.io/personal-workbench/**

用电脑浏览器、手机浏览器直接打开这个网址即可，无需登录，数据存在你本地浏览器（localStorage）。

## 已上线内容
- 四大模块：体重管理、心情指数、工作情况、月度总览
- 体重分析：热量超标 / 低热量双线归因，联动心情与工作，热量失控预警
- 排卵期储水等生理期水位模型
- 数据导入 / 导出（JSON、CSV，支持合并或替换）
- 云同步（需配合云端后端，静态站本身不依赖）
- PWA 可“添加到主屏幕”当 App 用

## 技术状态
- 纯静态站点（相对路径、Service Worker scope `./`、含 `.nojekyll`），GitHub Pages 直接托管，无后端依赖
- 仓库：`Lulu-hub-44/personal-workbench`（公开仓库，`master` 分支根目录为网页源）

## 关于那把 GitHub 钥匙（token）
- 部署已结束，**这把钥匙后续不再需要**——站点运行不依赖它。
- 建议现在去 github.com → Settings → Developer settings → Personal access tokens → Tokens (classic)，把名为 `wb` 的 token **Delete / Revoke** 掉，更安全。
- 删掉后网站照常访问，不受影响。

## 以后怎么更新
改代码后告诉我“重新部署”，我会用同样方式 push 并等 Pages 重新构建（约几十秒生效）。
